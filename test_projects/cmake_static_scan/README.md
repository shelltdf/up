# cmake_static_scan

包含 **`CMakeLists.txt`** 的极简多目标示例（`cg_foo` / `cg_bar`），用于**人工阅读**或文档对照。

**说明**：仓库已移除 **`up reverse`** 与 **`up project`** 等逆向子命令；本目录**不再**附带可重复的 `up` 扫描验证命令行。若需从 CMake 工程迁移到 `up`，请按 **`doc/zh/getting-started.md`** 手写 **`package.xml` / `target.xml`**。
