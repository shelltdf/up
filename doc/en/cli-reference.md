# CLI reference (entry)

> **Documentation index**: **[../README.md](../README.md)**

The full, detailed **`gz` CLI** reference (every flag, cwd semantics, intermediate directories, exit codes, and copy-paste examples) is maintained in Chinese:

**[../zh/cli-reference.md](../zh/cli-reference.md)**

Also see **[../zh/user-manual.md](../zh/user-manual.md)** (Section **0** for the **`_build` / `cmake -B`** path note) and **`gz --help`**. The **`configure`** section in **`zh/cli-reference.md`** documents **`GZ_TARGET_DYNAMIC_LIBRARY`** and **`target.xml` `type="library"`** (see **`zh/package-target-xml-spec.md`** §3.1). **`pack`** today only archives the install tree; it does **not** emit **`package.xml` / `target.xml`** (see **`zh/package-target-xml-spec.md`** §8 for current vs planned redistribution XML).
